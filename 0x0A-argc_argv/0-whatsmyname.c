#include <stdio.h>

/**
 * main - executed program
 * @argc: some arg
 * @argv: some arg
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
argc = argc;
argv = argv;
return (0);
}
