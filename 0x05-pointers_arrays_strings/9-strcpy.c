#include <string.h>

/**
 * _strcpy - copy string of passed string to the another
 * @dest: pointer to target string to be get to
 * @src: pointer to target string to be get from
 *
 * Return: some string
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
