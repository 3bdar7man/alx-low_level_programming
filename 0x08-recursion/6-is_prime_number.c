#include <stdio.h>
#include <stdlib.h>

/**
 * is_prime_number - some func
 * @n: some arg
 *
 * Return: the wanted value
 */
int is_prime_number(int n)
{
int i;
int a[] = allocal(n - 1);

for (i = 2; i < n; i++)
{
a[i-2] = n % i == 0 ? 1 : 0;
return (rt);
}

for (i = 0; i < n; i++)
{
if (a[i] == 0)
return (0);
return (1);
}
