#include <stdio.h>
#include <math.h>
#include <stdlib.h>

/**
 * _pow_recursion - like pow
 * @x: some arg
 * @y: some arg
 *
 * Return: the wanted value
 */
int _pow_recursion(int x, int y)
{
return (pow(x, y));
}
