#include <string.h>

/**
 * _memcpy - like memcpy
 *@dest: some arg
 *@src: some arg
 *@n: some arg
 *
 * Return: the wanted value
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
return (memcpy(dest, src, n));
}
