#include <string.h>

/**
 * _memset - like memset
 *@s: some arg
 *@b: some arg
 *@n: some arg
 *
 * Return: the wanted value
 */
char *_memset(char *s, char b, unsigned int n)
{
return (memset(s, b, n));
}
