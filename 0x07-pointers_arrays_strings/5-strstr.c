#include <string.h>

/**
 * _strstr - like strstr
 *@haystack: some arg
 *@needle: some arg
 *
 * Return: the wanted value
 */
char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
